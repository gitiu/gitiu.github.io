# 响应式修复与设计说明（2026-09-09）

## 撤回悬停调整（20260910m）

按用户要求撤回 20260910e 的整轮悬停调整，恢复 20260910d 的按钮、文章、友链和文字链接交互样式。保留此前的统计摘要、建站时间轴与深色配色，使用新资源版本刷新缓存。

## 建站时间轴（20260910d）

桌面时间轴改为上方连续细线、下方日期标签：空心起点代表建站，强调色实心终点加柔和外环代表现在。日期展示完整的 2019.10.24，累计天数适度加大。线条随浅色 / 深色主题配色，无循环动画；时间轴为日期连接，不表示进度百分比。手机保持单行天数，不增加页头高度。预渲染与 JS 回退模板同步，126 组响应式检查通过，另检查 320、390、1440px 浅色和深色截图。

## 统计摘要第二次调整（20260910c）

去掉统计区域内嵌卡片、标题与装饰省略号，直接融入个人介绍。桌面用笔与对话线条图标区分记录和评论，26px 数字配合小标签；建站月份与天数由一条细时间线连接。手机压缩为无边框的一行，数字 16px、图标 14px，隐藏时间线与起始日期；相较上一版减少约 24px 高度。浅色与深色截图已检查，126 组响应式检查通过。

## 生活存档与深色配色（20260910b）

首页统计改成生活存档摘要：桌面采用小笔记卡，展示记录、评论、建站月份及天数；860px 及以下隐藏卡片标题和冗余前缀，变为一行轻量信息，避免三列大数字挤占手机首屏。预渲染与 JS 回退模板保持一致，回退天数由建站日期计算，移除旧的固定数字。

深色模式使用炭灰背景 #1c1e22、卡片 #25282d、灰白文字 #dedfe3 与蓝灰强调色 #aabbd0。按钮、焦点、友链默认图标、小猫及标语 SVG、照片灯箱同步配色；浅色模式保留暖白和棕色。首屏主题注入同步新背景，标志资源附带版本参数以更新缓存。

验证：126 组页面与视口检查通过，无横向溢出或页脚布局回归；桌面首页在不同视口高度下保持 732px 内容高度。检查了 1440、390、320px 首页浅色 / 深色截图，以及归档、友链、时间线深色显示。延迟主脚本 700ms 的 12 个主题 / 跳转状态测试通过，未采样到错误背景色。手机检查使用浏览器模拟视口。

## 已定位的原因

线上仍引用 `20260818a` 版本，与本地复现一致。原来的 `body` 设置视口最小高度，`#content` 使用 `flex: 1 0 auto`；主页网格又按视口高度计算最小高度，并在 780px 封顶。两层高度规则不一致。

在 2560×1440 的浏览器视口中，原网格高 780px，外层内容高约 1291px，卡片底部到页脚顶部约 525px。与此同时，文章条目和个人简介使用 flex 扩张及均匀分配，导致内容被拉开。短文章页也会被外层撑高。

## 布局约定

- 首页、时间线和友链按内容排版，页脚跟随内容；桌面间距 24px，平板和手机 20px，单列首页 16px。文章阅读页使用至少一屏的外层布局，页脚靠底，但正文不伸展。
- 页面最大外宽 1160px，文章阅读页最大外宽 960px。宽屏不会继续放大卡片。
- 860px 及以下改为单列；首页导航保持一行四项，主要按钮触控区域为 44px。
- 网格列允许收缩，长标题可换行；友链网格最小宽度不超过容器。代码块在内部横向滚动，图片保留比例。
- 不使用页面级 `overflow-x: clip` 掩盖越界。保留滚动条位置，减少长短页面切换时的横向跳动。

## 视觉与交互

保留猫咪元素、暖色卡片和深色主题。浅色页面采用暖灰背景、纸白卡片、棕色强调色；文章标题、摘要、日期逐级减弱。页脚改为轻分隔线。说说按年份分组，日期与正文分别对齐。搜索增加中文提示和可访问名称；主题按钮支持键盘操作，并同步图标。自动主题跟随系统深浅色偏好。无章节的短文隐藏空目录按钮。

## 验证

使用真实 Chromium 浏览器在本地静态站点检查。9 类页面 × 14 个视口，共 126 组：主页、第二页、标签、说说、关于、友链、短文、长文、图片文章。宽度为 320、360、390、430、600、768、820、860、861、1024、1280、1440、1920、2560px。

- 页面无横向溢出，主要容器无越界，内容与页脚同宽，间距不超过 28px。
- 固定宽度 1440px，视口高度从 600、819、820、900、1440 到 2160px，主页网格均约 653px 高。
- 搜索结果、无结果状态、深浅色与系统自动主题正常。
- 320px 下测试超长连续文本和长代码行：页面不越界，代码块内部可滚动。图片文章的三张图片加载正常，显示比例保持一致（允许像素舍入误差）。

这些是浏览器视口模拟结果，不是实体手机或所有浏览器内核的逐一测试。第三方评论服务仍依赖其网络可用性。

## 本地复查

在仓库根目录启动静态服务：

```powershell
python -m http.server 4173 --bind 127.0.0.1 --directory docs
```

另开终端，使用 Playwright CLI 执行回归。脚本返回 JSON，`failures` 和 `errors` 应为空，并生成代表页面截图：

```powershell
npx --yes --package @playwright/cli playwright-cli -s=responsive open http://127.0.0.1:4173 --headed
npx --yes --package @playwright/cli playwright-cli -s=responsive run-code --filename=tools/responsive-audit.js --raw
```

维护入口是 `static/gitiu-bento.css` 与 `static/gitiu-bento.js`。已同步 `docs/` 产物及预渲染脚本，资源版本为 `20260910m`。改动版本时同时更新 JS 和预渲染脚本中的版本常量，再运行 `python static/gitiu-prerender.py` 同步页面引用。GitHub Actions 沿用现有构建流程。


## 手机首页密度调整（20260909b）

单列布局下将个人卡片改为轻量页头：头像与站名、横排统计、一行导航；长简介仍在桌面显示。四篇文章全部保留，第一篇展示最多两行摘要，其他篇目展示标题和日期。完整内容仍通过各文章链接阅读。说说入口改为横条，保留全部动态链接。

390×844 视口下，首页总高由约 1256px 减少到 841px（约 33%）；个人区从 336px 减至 152px，文章区从 661px 减至 487px，说说区从 122px 减至 76px。没有固定页面高度，也没有嵌套滚动区，长标题仍可自然换行。桌面布局保持原样。

再次完成 126 组回归，均通过；四个导航目标保持 44px 高，深色及自动主题下也检查了页头背景与日期样式。

## Logo 与时间线优化（20260909c）

首页站名改为原创 SVG 图形字标，提供猫耳字标、折页 G、猫咪印记三款，默认猫耳字标。每款均有独立深浅色文件，跟随站点主题切换；不依赖在线字体。`logo-preview.html` 提供比较、下载和实际首页预览，`index.html?logo=fold` / `?logo=seal` 可临时预览另外两款。维护文件在 `static/logos/` 与 `static/pages/logo-preview.html`。

说说页按年份分组，保留全部 7 条动态，日期独立对齐，摘要最多三行，整行可进入全文。桌面使用年份侧栏，手机改为分组标题。移除原有发光节点和气泡装饰，统一暖色卡片、细分隔线与深色背景。

新版 390px 首页实测总高 868px，比最初约 1256px 减少约 31%；Logo 下方保留中文副标题。重新验证 126 组布局均通过，页脚间距为 16–24px。三款 Logo 的六个文件加载正常，系统自动主题切换验证通过。源码与 `docs/` 生成资源已同步。

## 归档与搜索（20260909d）

归档采用年份分组、日期列和轻量分类筛选。恢复原生 form 与 submit 按钮，点击和回车共用提交逻辑，删除隐藏并禁用原搜索按钮的旧规则。替换 SVG 模拟按钮，搜索状态和无结果提示使用安全的文本节点。使用 MutationObserver 等待引擎异步加载列表，替代定时重试；分类、搜索和 URL 标签保持可恢复。

`tools/archive-audit.js` 验证 320、390、768、1440px 下按钮点击、回车搜索、无结果、分类筛选与恢复全部内容，并生成截图。另完成 126 组通用响应式回归，全部通过。资源版本已升级并同步生成产物。

## 友链重构（20260909e）

友站采用三列 / 两列 / 单列自适应卡片，本站从友站列表独立为可复制名片。申请要求和留言入口完整保留，规则使用原生 details 折叠。尚无实际内容的圈子和项目占位不再显示，文章目录在友链页隐藏。

移除 Google favicon 服务依赖，直接尝试各站 `/favicon.ico`。本地 SVG 猫咪窗台标记立即显示，加载失败或图标任一边小于 32px 时继续使用备用标记；合格图标以 contain 保持比例。备用标记配有站名首字和稳定配色，不会出现空图或第三方默认地球。独立 SVG 为 `static/logos/gitiu-neighbor.svg`。

`tools/friends-audit.js` 覆盖 320–2560px 六个视口、全部图标失败时的备用显示、申请折叠、深色主题及页脚间距。另用受控图片响应验证 16px 图标被替换、64px 图标正常显示，并验证复制信息的内容。

## 首页 Slogan 字标（20260909f）

「我的生活际遇」改为六个手绘圆头线条汉字的 SVG 字标，置于 Logo 下方居中，桌面宽 178px、单列布局宽 160px。浅色暖棕、深色浅金，自动主题同步切换。文件为 `static/logos/gitiu-slogan.svg` 和 `gitiu-slogan-dark.svg`，图片 alt 保留完整中文；不依赖用户设备字体。

已检查 320、390、860、1440px 和深色模式，未发现横向溢出。390px 首页高度约 875px，比上一版增加约 7px。预渲染模板、JS 回退模板及 docs 资源均已同步。

## 全站按钮（20260909g）

按钮统一为 11px 圆角、至少 44px 触控高度。搜索和留言为暖棕主按钮；导航、返回、复制及更多入口为浅底描边；分类使用柔和的选中色；主题、目录和看图操作使用统一尺寸图标按钮。悬停、按下、禁用和键盘焦点由同一组变量控制，深色与自动模式同步配色，主题图标不再保留单独的橙色。

JS 的 `enhanceControls()` 为各控件添加 `data-gitiu-control` 角色，异步生成的插件控件也会接入。目录支持键盘打开和 Escape 关闭，代码复制增加可访问名称及 Enter / Space 操作，隐藏的看图控件设为 inert。评论加载显示中文状态，第三方评论加载完成后仍能隐藏入口。代码复制控件布局不再依赖外部工具 CSS，目录深色文字与分隔线使用主题变量。

验证覆盖 7 类页面 × 3 个视口的按钮尺寸与布局，另检查搜索点击/回车、分类、目录键盘、代码复制反馈、看图关闭与主题键盘切换。通用 126 组响应式检查通过。测试脚本为 `tools/buttons-audit.js`、`tools/archive-audit.js` 与 `tools/responsive-audit.js`。代码复制与留言入口测试使用受控替身验证触发；没有提交真实评论。

## 跳转闪白修复（20260909h）

复现：自动主题配合系统深色时，系统色标记直到主 JS 执行才设置。人为延迟 JS 700ms，时间线在约 372ms 首次绘制浅色背景，约 744ms 才切为深色。原来的 ensureDesignCssLast 还会重新插入已加载的主题 link，可能产生短暂的样式失效。

预渲染现在在所有主题页面的 head、字符编码声明之后，注入同步主题判断与最小首屏背景 / color-scheme CSS，位于外部样式之前。主脚本保持已加载主题样式表原位，通过前移后续模板样式维护级联顺序。注入可重复执行，不产生多个片段。

`tools/theme-paint-audit.js` 延迟主脚本 700ms，实际点击“时间线 → 文章 → 首页”，覆盖浅色、深色、自动跟随系统深色/浅色，共 12 个页面状态。修复后帧采样始终保持预期背景色；通用 126 组布局检查通过。构建流程会自动应用此修复到新文章。

## 照片灯箱与短文页脚（20260910a）

`static/plugins/lightbox.js` 已重写为原生 dialog 照片查看器，采用深色画布、暖色工具栏、缩略图、照片计数和原图入口。支持按钮 / 键盘切图、1–4 倍缩放、滚轮 / 双击缩放、拖动、手机双指缩放和左右滑动；关闭恢复阅读位置与焦点，背景滚动锁定。只收集正文照片，排除友链图标及数学公式。加载失败提供重试，异步加载序号避免快速切图时显示旧图。已对灯箱脚本加入版本参数，防止浏览器继续使用旧插件。

照片优先使用站内优化大图，原图入口保留 `data-full-src` 指向的原始资源。横竖图按比例适应窗口，不强制裁切；只有缩略图采用裁切。移动端隐藏冗余操作提示，控件保留 44px 点击区域。

短文章 / 说说正文页的 body 使用 min-height:100svh 和 flex 列布局，content 不伸展，footer 的自动上边距吸收剩余空间。长文自然向下排版，首页及友链保持原有高度策略。320、390、1440px 下，视口高度从 600 到 1440px，短文卡片高度不变，页脚始终位于底部。

验证：`tools/lightbox-audit.js` 实测缩放拖动、缩略图、横竖比例、键盘关闭、焦点和滚动恢复、浏览器模拟触摸双指缩放（1.8×）及滑动切图、单图和加载失败重试。另检查阅读中段关闭灯箱前后 scrollY 一致，通用 126 组响应式布局检查通过。触摸测试是浏览器模拟，并非实体手机测试。
